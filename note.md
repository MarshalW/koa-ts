#  note

```bash
mkdir koa-ts
cd koa-ts

npm init -y
npm install koa koa-router koa2-cors koa-bodyparser
npm install @types/koa @types/koa-router @types/koa2-cors @types/koa-bodyparser
npm install webpack webpack-cli

touch webpack.config.js
# 填写webpack.config.js

# package.json + "build": "webpack"

npm install nodemon ts-node -D

touch .editorconfig

mkdir src

```